// app/components/navbar/Navbar.tsx
"use client";

export default function Navbar() {
  // Por ahora no mostramos ninguna barra extra en /admin:
  // todo el contexto del panel se maneja con el header global (botón PANEL).
  return null;
}